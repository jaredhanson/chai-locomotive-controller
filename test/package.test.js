/* global describe, it, expect */

var pkg = require('..');

describe('chai-locomotive-controller', function() {
  
  it('should export object', function() {
    expect(pkg).to.be.an('object');
  });
  
});
